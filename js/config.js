// Point this at your running FastAPI backend.
// Default assumes the backend is running locally on port 8000
// (see backend/README or run: uvicorn app.main:app --port 8000)
const API_BASE = window.MNVISION_API_BASE || "http://localhost:8000";
